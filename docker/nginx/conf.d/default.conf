upstream php {
    server php:9000;
}

upstream react {
    server react:3000;
}

server {
    listen 80;
    server_name localhost;

    root /var/www/html/docker-bookingsystem/laravel/public;

    # REDIRECT: /docker_bookingsystem → hazree.bookingsystem.test
    location /docker_bookingsystem {
        return 301 http://hazree.bookingsystem.test;
    }

    # API routes go to Laravel
    location /api {
        fastcgi_pass php;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root/index.php;
        fastcgi_param SCRIPT_NAME /index.php;
        fastcgi_param REQUEST_URI $request_uri;
        include fastcgi_params;
    }

    # React/Vite owns every non-API route
    location / {
        proxy_pass http://react;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
